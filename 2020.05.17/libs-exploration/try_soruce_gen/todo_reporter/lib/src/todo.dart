// An annotation in dart is just a class with a const constructor

/// Make the annoted class be tracked as todo
class Todo {
  final String name;
  final String url;

  const Todo(this.name, {url: this.url} : assert(name != null);
}
